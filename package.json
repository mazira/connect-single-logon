{
  "name": "connect-single-logon",
  "version": "0.0.1",
  "description": "Allow only a single logon per user",
  "keywords": [ "connect", "single login", "exclusive login", "middleware", "web"],
  "author": "Uday Verma (uday.karan@gmail.com)",
  "dependencies": {
  },
  "main": "index.js",
  "devDependencies": {
    "connect": "*",
    "mocha": "*",
    "should": "*"
  },
  "engines": {
    "node": ">= 0.5.0"
  }
}

